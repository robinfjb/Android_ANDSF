/*package com.chinamobile.android.connectionmanager.receiver;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

public class AlarmReceiver extends BroadcastReceiver {
	public static final String ALARMACTION = "packagename.ACTION"; //packagename is com.whatever
	@Override
	public void onReceive(Context context, Intent intent) {
	    if(intent.getAction().equals(ALARMACTION)) {
	        // here you call a service etc.
	    }
	}

}
*/